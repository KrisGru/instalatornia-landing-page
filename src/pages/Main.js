import React from "react";
import { Home, About, Offer, Gallery, Contact } from "./";

const Main = () => (
	<main>
		<Home />
		<About />
		<Offer />
		<Gallery />
		<Contact />
	</main>
);

export default Main;
